apply plugin: 'java'
apply plugin: 'jacoco'
apply plugin: 'application'


mainClassName = 'NewFinal.Main'

repositories {
    jcenter()
}

dependencies {
    testImplementation     'junit:junit:4.13'
      compile fileTree(dir: 'libs', include: '*.jar')
}
run.standardInput = System.in;
